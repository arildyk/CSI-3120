makeActiveBinding("randomNo", function() rnorm(5), .GlobalEnv)
#The set. seed() function in R is used to create reproducible results 
#when writing code that involves creating variables that take on random values.
#By using the set. seed() function, you guarantee that the same random values 
#are produced each time you run the code.